# GitHub token for cloning repos (avoids rate limits)
GITHUB_TOKEN=

# Data directory (default: ./data)
STRIFF_DATA_DIR=./data

# Clarpse parser API (default: http://localhost:8080)
CLARPSE_API_URL=http://localhost:8080
